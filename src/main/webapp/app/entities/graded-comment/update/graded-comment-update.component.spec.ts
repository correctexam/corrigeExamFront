describe('GradedComment Management Update Component', () => {
  beforeEach(() => {});
  describe('ngOnInit', () => {
    it('Should load textComment on init', () => {
      expect(true);
    });
  });
});
