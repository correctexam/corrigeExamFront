export function loadImageTensor(imagePath: string): any {
  // Implémentation
}

export function runInference(data: any, modelPath: string): any {
  // Implémentation
}

export function preprocessImage(
  image: any,
  channelNb: number,
  padValue: number,
  padWidthRight: number,
  padWidthLeft: number,
  mean: number,
  std: number,
  targetHeight: number,
): any {
  // Implémentation
}

export function loadImageTensorFromBase64(base64: string): any {
  // Implémentation
}

export function preprocessImageTensor(
  imageTensor: any,
  channelNb: number,
  padValue: number,
  padWidthRight: number,
  padWidthLeft: number,
  mean: number,
  std: number,
  targetHeight: number,
): any {
  // Implémentation
}
